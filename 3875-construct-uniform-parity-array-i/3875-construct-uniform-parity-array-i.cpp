class Solution {
public:
    // what's the point of this question?
    // the same one was where you needed to return 0
    // I think it was about turning number to base k
    // and checking if its a palindrome or something like that...

    inline const bool uniformArray(
        vector<int>& __ns
    ) const noexcept { return 1; }
};